import React from 'react';

type Props = {

};

const WorkflowConf: React.FC<Props> = (props) => {
  const { } = props;
  return (
    <div className="workflow-conf">
      WorkflowConf
    </div>
  );
};

export default WorkflowConf;