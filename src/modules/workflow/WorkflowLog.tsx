import React, { useState } from 'react';

type Props = {};

const WorkflowLog: React.FC<Props> = (props) => {
  // const { } = props;

  return (
    <div>
      1
    </div>
  );
};

export default WorkflowLog;