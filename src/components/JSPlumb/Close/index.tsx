import React, { useState, useEffect, CSSProperties, SyntheticEvent, Children, cloneElement, PureComponent } from 'react';

type Props = {

};

//@ts-ignore
const Close: PureComponent<Props, null> = ({ props, state }: any) => {


  useEffect(() => {

  }, []);

  return (
    <div>

    </div>
  );
};

export default Close;